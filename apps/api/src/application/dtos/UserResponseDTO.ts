import { UserResponse } from '../../domain/entities/User';

/**
 * User Response DTO — re-export from domain entity for convenience.
 * This ensures the application layer uses the canonical UserResponse type.
 */
export type { UserResponse };
