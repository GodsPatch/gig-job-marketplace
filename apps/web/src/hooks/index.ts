/**
 * Placeholder for custom React hooks.
 *
 * TODO M2: Add auth hooks:
 * - useAuth() — access current user, login/logout
 * - useRequireAuth() — redirect to login if not authenticated
 */
export {};
